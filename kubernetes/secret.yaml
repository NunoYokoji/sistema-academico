# Informação sensível da aplicação (seção 17).
# stringData é gravado em base64 pelo próprio Kubernetes.
#
# Este Secret está no repositório apenas porque é um exercício acadêmico.
# Em produção ele ficaria fora do versionamento (Sealed Secrets, Vault, etc.).
apiVersion: v1
kind: Secret
metadata:
  name: sistema-academico-secret
  labels:
    app.kubernetes.io/part-of: sistema-academico
type: Opaque
stringData:
  DB_PASSWORD: postgres
